<?php

it('returns available organization workspaces for a user linked to multiple organizations instead of collapsing the session to the first membership')->todo();

it('allows switching the active organization context without creating duplicate user accounts')->todo();

it('keeps event-only invitations attachable to an existing platform user identified by whatsapp or email')->todo();

it('returns four event workspaces for the same dj across different partner organizations with per-event capabilities')->todo();

it('groups event workspaces by partner organization without granting organization-wide access to the dj')->todo();

it('uses the selected event context to scope media moderation wall and play endpoints for an event-scoped user')->todo();

it('returns enough event workspace metadata for frontend filters without exposing unrelated organizations')->todo();

it('redirects event-only sessions through an active event context instead of a current organization')->todo();
