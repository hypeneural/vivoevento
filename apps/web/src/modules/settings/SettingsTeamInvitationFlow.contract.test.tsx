import { describe, it } from 'vitest';

describe('Settings team invitation v2 contracts', () => {
  it.todo('shows a send via WhatsApp option and sends the explicit delivery preference to the API');
  it.todo('renders pending invitations separately from active team members');
  it.todo('shows the generated invitation link for manual copy when delivery is skipped or fails');
  it.todo('keeps owner transfer outside the generic add member flow');
  it.todo('uses simple company-wide vs single-event access presets instead of exposing a raw permission matrix to the cerimonial');
});
