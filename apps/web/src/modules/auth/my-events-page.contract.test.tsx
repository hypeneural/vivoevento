import { describe, it } from 'vitest';

describe('/my-events contracts', () => {
  it.todo('groups event cards by partner organization');
  it.todo('uses simple cards with partner event date role summary and primary actions instead of a dense data table');
  it.todo('filters event cards by search text partner status period role and capability');
  it.todo('sorts event cards by upcoming date recent date partner name and event title');
  it.todo('shows only media action for a media-viewer event access');
  it.todo('shows bride-friendly empty states and plain-language permission summaries without exposing raw permission keys');
  it.todo('shows media moderation wall and play actions for an operator event access');
  it.todo('shows only the actions and tabs allowed in the selected event context');
  it.todo('hides clients billing whatsapp settings audit and organization team navigation for event-only users');
});
