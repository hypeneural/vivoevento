/**
 * Re-export useTheme from provider for convenience.
 * Import from '@/shared/hooks/useTheme' in modules.
 */
export { useTheme } from '@/app/providers/ThemeProvider';
export type { ThemeMode, ResolvedTheme } from '@/app/providers/ThemeProvider';
